<% if (flash != null) { %>
    <div class="alert flash <%= flash.error ? "bg-danger" : "bg-success" %>" role="alert">
        <h4 class="text-center text-dark"><%= flash.message %></h4>
    </div>
<% } %>